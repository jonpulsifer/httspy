---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: httspy
  namespace: monitoring
  labels:
    app: httspy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: httspy
  template:
    metadata:
      labels:
        app: httspy
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
      containers:
        - name: httspy
          image: ghcr.io/jonpulsifer/httspy:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          resources:
            requests:
              cpu: 100m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
---
apiVersion: v1
kind: Service
metadata:
  name: httspy
  namespace: monitoring
spec:
  selector:
    app: httspy
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
---
# ReferenceGrant allows httspy to be referenced by HTTPRoute in app-namespace
apiVersion: gateway.networking.k8s.io/v1beta1
kind: ReferenceGrant
metadata:
  name: httspy-app
  namespace: monitoring
spec:
  from:
  - group: gateway.networking.k8s.io
    kind: HTTPRoute
    namespace: app-namespace
  to:
  - group: ""
    kind: Service